{
  "name": "remix-etherscan-mcp",
  "version": "0.4.0",
  "description": "MCP server: Etherscan API v2 + remixd — full coverage for Ethereum dev and audit workflows",
  "type": "module",
  "bin": {
    "remix-etherscan-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@types/diff": "^8.0.0",
    "diff": "^9.0.0",
    "ethers": "^6.16.0",
    "ws": "^8.20.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
